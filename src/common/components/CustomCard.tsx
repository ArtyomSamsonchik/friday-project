import React from 'react'

import { styled } from '@mui/material'

import { CustomPaper } from './CustomPaper'
export const CustomCard = styled(CustomPaper)({
  position: 'relative',
  width: '100%',
  maxWidth: 360,
})
